# Copy to database/.env. The Prisma configuration reads this URL directly.
# Use the migration role, not the application or PostgreSQL bootstrap role.
MIGRATION_DATABASE_URL=postgresql://homehub_migrator:REPLACE_WITH_URL_ENCODED_MIGRATOR_PASSWORD@127.0.0.1:5432/homehub?schema=public
